services:
      investment-platform:
        build:
          context: .
          dockerfile: Dockerfile
        container_name: investment_platform_trove
        ports:
          - "8080:8080"
        environment:
          - SPRING_PROFILES_ACTIVE=prod
        restart: unless-stopped